diff --git a/src/decompiler/heritage.cc b/src/decompiler/heritage.cc
index 7f8bb2b..5701d29 100644
--- a/src/decompiler/heritage.cc
+++ b/src/decompiler/heritage.cc
@@ -1610,9 +1610,10 @@ bool Heritage::refinement(const Address &addr,int4 size,const vector<Varnode *>
   // Alter the disjoint cover (both locally and globally) to reflect our refinement
   LocationMap::iterator iter = disjoint.find(addr);
   int4 pass = (*iter).second.pass;
-  disjoint.erase(iter);
+  // disjoint.erase(iter);
   iter = globaldisjoint.find(addr);
-  globaldisjoint.erase(iter);
+  // globaldisjoint.erase(iter);
+
   Address curaddr = addr;
   int4 cut = 0;
   int4 intersect;
