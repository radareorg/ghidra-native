---
name: ci

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  dist-zip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Distribution Zip
        run: make dist
      - name: Extract r2 version
        run: echo "##[set-output name=Version;]$( cat VERSION )"
        id: Extract
      - name: Pub
        uses: actions/upload-artifact@v2
        with:
          name: ghidra-native-${{ steps.Extract.outputs.Version }}.zip
          name: ghidra-native-${{ steps.Extract.outputs.Version }}.zip
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Patching
        run: make patch
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Patching
        run: make patch
#  build-windows:
#    runs-on: windows-latest
#    steps:
#    - uses: actions/checkout@v2
#    - name: Windows Dependencies
#      run: pip install meson ninja
#    - name: Building Sdb
#      run: meson build && ninja -C build
#    - name: Pub
#      uses: actions/upload-artifact@v2
#      with:
#        path: build/*.exe
